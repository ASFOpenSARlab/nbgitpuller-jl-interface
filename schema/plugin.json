{
  "jupyter.lab.shortcuts": [],
  "title": "nbgitpuller-jl-interface",
  "description": "nbgitpuller-jl-interface settings.",
  "type": "object",
  "properties": {
    "reloadWidget": {
      "type": "boolean",
      "title": "Reload Widget",
      "description": "Reloads widget, useful if you want to change your settings",
      "default": true
    },
    "repos": {
      "type": "array",
      "title": "Repositories",
      "items": {
        "type": "object",
        "properties": {
          "repoUrl": { "type": "string" },
          "branch": { "type": "string" },
          "destPath": { "type": "string" }
        }
      },
      "description": "List of repositories monitored by NBGitpuller",
      "default": []
    },
    "rank": {
      "type": "number",
      "title": "Relative Rank",
      "description": "Relative rank of feature. The bigger the number, the further to the right.",
      "default": 1000
    },
    "probeInterval": {
      "type": "number",
      "title": "Probe Interval",
      "description": "The interval in ms between probes for repository updates.",
      "default": 900000
    }
  },
  "additionalProperties": false
}
